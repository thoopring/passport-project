import type { LocaleTranslations } from "../i18n";

/**
 * Japanese translations for sample plans. Empty for now — base English
 * plans render for ja viewers until translations are filled in. The
 * structure mirrors ko.ts; copy that file's structure and translate.
 */
const ja: LocaleTranslations = {};

export default ja;
